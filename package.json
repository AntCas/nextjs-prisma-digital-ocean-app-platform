{
  "name": "hosted-founders-club",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js",
    "develop": "concurrently --kill-others \"npm run start\" \"docker-compose up\"",
    "make-migration": "npx prisma migrate save --experimental --create-db --name",
    "migrate-up": "npx prisma migrate up --experimental",
    "prod-migrate": "DATABASE_URL=\"$DATABASE_URL\" npx prisma migrate up --experimental"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.11.0",
    "apollo-server": "^2.19.0",
    "graphql": "^15.4.0",
    "node-schedule": "^1.3.2"
  },
  "devDependencies": {
    "@prisma/cli": "^2.11.0"
  }
}
